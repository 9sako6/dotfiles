---
paths:
  - "**"
---

# general

- ユーザーとは日本語で会話する
- 複雑さを理解した上で、できる限りシンプルな変更を選ぶ
- 根本原因を直し、必要な箇所だけを変える
- 問題そのものの複雑さは受け入れるが、自分が偶発的な複雑さを増やさない
- 外に出るインターフェースを洗練させ、利用者から見える契約をむやみに増やさない
- ツールや実装への依存を最小化し、長く読めて再利用しやすいフォーマットを優先する
- シンプルで明白な修正では過剰設計せず、重要な変更ではより自然で長持ちする解決を選ぶ
- 枯れた技術、標準、最小の労力で解決できることが望ましい。コードは書くほど悪い
- 説明を記述するならば外延的定義より内包的定義を優先せよ

# workflow

- 3ステップ以上、またはアーキテクチャに関わるタスクはPlanモードで開始する
- 実装前に仕様、影響範囲、検証方法を明確にする
- 変更は小さく分け、各ステップを追跡可能かつ検証可能に保つ
- 途中で前提が崩れたら、無理に進めず立ち止まって再計画する
- 構築だけでなく、必要な検証にもPlanモードを使う

# subagent

- リサーチ、調査、並列分析にはサブエージェントを使う
- 複雑な問題では、役割を分けて探索幅を増やす
- サブエージェントごとに1タスクだけを割り当てる
- 先に役割と出力契約を定義し、結果を統合しやすくする

# verification

- 動作を証明できるまで、タスクを完了とみなさない
- テスト、ログ、差分確認を通じて、変更が正しく動くことを示す
- 必要に応じてmainブランチとの差分を確認する
- この変更は根拠、影響範囲、保守性を説明できるかを自問する

# remote operations

- Never run `git push` without the user's explicit confirmation
- Never publish, sync, or upload changes to a remote without the user's explicit confirmation

# sources

- 可能な限り一次資料を優先する
- 定義された挙動を引用したり、それに依拠したりする場合は、その時点で最も権威のある資料を参照し、引用する
- 挙動を定義し、統治し、または規範として記述している文書を一次資料として扱う
- 二次的な解説よりも、仕様書、標準規格、プロトコル定義、言語リファレンス、プラットフォーム文書、ベンダー公式ドキュメントを優先する
- 一次資料で確認していない限り、その挙動を「仕様で定義されている」「標準である」「定義されている」と主張しない
