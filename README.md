# .zsh


## Settings

```
~/
 ├ .zsh/
 │ └ .zshrc
 └ .zshenv
```

To read `~/.zsh/.zshrc`, write this in `~/.zshenv`:

```
export ZDOTDIR=$HOME/.zsh
```

## Requirements
- zsh-completions

```
brew install zsh-completions
```
